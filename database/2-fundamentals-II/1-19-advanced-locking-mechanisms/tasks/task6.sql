-- Опис:
-- У системі обробки транзакцій кілька процесів можуть одночасно намагатися оновити статус однієї і тієї ж транзакції 
-- (наприклад, з 'pending' на 'completed'). Щоб уникнути конфліктів і race conditions, необхідно заблокувати 
-- відповідний рядок таблиці transactions перед оновленням.

-- 📋 Завдання:
-- Заблокуйте транзакцію з id = 1, а потім змініть її статус на 'completed'.

BEGIN;
-- Рішення

-- Увага: не виконуйте COMMIT, щоб транзакції залишались заблокованими